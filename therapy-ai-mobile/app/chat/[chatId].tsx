import React from "react";
import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function ChatSessionScreen() {
  return (
    <SafeAreaView>
      <View>
        <Text>Chat Session</Text>
      </View>
    </SafeAreaView>
  );
}